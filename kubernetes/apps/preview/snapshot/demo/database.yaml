apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: demo-database-snapshot
  namespace: preview
spec:
  cluster:
    name: pr-17963-preview-database
  method: volumeSnapshot
  online: false
